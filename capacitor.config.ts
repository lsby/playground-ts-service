import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'lsby.playground.ts.service',
  appName: '@lsby/playground-ts-service',
  webDir: 'dist/web',
}

export default config
